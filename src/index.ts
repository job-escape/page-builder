// Default entry re-exports the server-safe surface.
// For client-only things (providers, hooks, BuilderClient, registry components)
// import from "@job-escape/page-builder/client".
export * from "./server";
